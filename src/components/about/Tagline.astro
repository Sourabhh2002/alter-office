---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class:list={[
    "font-HamonBold text-[clamp(1.125rem,0.982rem+0.714vw,1.625rem)] leading-[clamp(1.375rem,1.232rem+0.714vw,1.875rem)] opacity-90 sm:w-[732px]",
    className,
  ]}
>
  <slot />
</div>
